# ⚠️ 占位符：生产发布前必须替换
#
# 当前值是开发机局域网 IP（个人电脑 WiFi 内网），微信小程序审核 / 真机用户访问会失败
# 生产部署步骤：
#   1. 将后端部署到有公网域名的服务器（必须 HTTPS，受微信小程序要求）
#   2. 在微信公众平台后台配置 request 合法域名
#   3. 替换下面的 URL 为你的 HTTPS 域名 + /api 后缀
#      示例：TARO_APP_API_BASE_URL=https://api.your-domain.com/api
#
# 详细发布文档：见项目根目录 .trae/documents/ 下的发布指南
TARO_APP_API_BASE_URL=https://REPLACE-WITH-YOUR-PRODUCTION-DOMAIN.COM/api
